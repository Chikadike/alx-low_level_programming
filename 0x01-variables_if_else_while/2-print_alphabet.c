#include <stdio.h>
/**
*main-prints the alphabet in lowercase, followed by a new line.
*Return:Always 0
**/
int main(void)
{
	char letter = 'a';

{
		putchar("Lowercase English Alphabets:\n");
		while leter 'a' <= 'z'
}
		putchar("char", letter);
		letter++;
	return (0);
}

