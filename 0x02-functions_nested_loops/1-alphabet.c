#include <stdio.h>

/**
*main-prints the alphabet, in lowercase
*Return:Always 0
*/

void print_alphabet('a'- 'z')
{
	char = 'x'
	x = 'a'
	for (x = 'a', x <= 'z', x++);
	while (x <= z)
	{		putchar("%d\n", x);
		x++;
	}
	print_alphabet('a-z');	
	return (0);
}
